# Core
anthropic>=0.40.0
openai>=1.50.0
pyyaml>=6.0
pydantic>=2.0

# Database
supabase>=2.0
psycopg2-binary>=2.9

# Async (asyncio is stdlib — do not pip install it)
aiohttp>=3.9

# Utilities
python-dotenv>=1.0
structlog>=24.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# Optional: working memory (Redis) — install separately if REDIS_URL is set
# redis>=5.0

# Optional: local LLM
# ollama  # install separately

# Optional: voice (Layla)
# deepgram-sdk>=3.0
# elevenlabs>=1.0
